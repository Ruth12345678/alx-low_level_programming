#include "main.h"
#include<stdio.h>

/**
 * rev_string: reverse string
 *
 * @s: input string
 *
 * Return: rturns value
 */

void rev_string(char *s)
{
	int a, b, c;
	char d;

	for (a = 0; s[a] != '\0'; a++)
		b = 0;

