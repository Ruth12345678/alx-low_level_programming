#include<stdio.h>
#include<string.h>

/*
 * main - prints out the specified string
 *
 * Return: Returns zero
 */

int main(void)
{
	char stra[250];

	strcpy(stra, "\"Programming is like building a multilingual puzzle");
	puts(stra);	
	return (0);
}
