/**
 *main - entry point
 
 return - always zero
 */
#include<stdio.h>
#include<string.h>
int main(void)
{
	char stra[250];

	strcpy(stra, "\"Programming is like building a multilingual puzzle");
	puts(stra);
	return (0);
}
