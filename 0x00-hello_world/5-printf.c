#include<stdio.h>

/**
 * main - returns a string
 *
 * return - returns zero
 */

int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", str);
	return (0);
}
