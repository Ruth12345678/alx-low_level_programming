#include<stdio.h>
#include<string.h>

/**
 * main - entry point
 *
 * Return: returns zero as a success value
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%i", a);
	}
	putchar('\n');
	return (0);
}
